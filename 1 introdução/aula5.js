// OPERADORES LÓGICOS

var sexo = 'M', idade = 23;

// AND (&&)
if (sexo == 'M' && idade > 18) {
    return 'Ok'
}


// OR (||)
if (sexo == 'M' || idade > 18) {
    return 'Ok'
}

// NOT (!== ou !=)
if (sexo !== 'M') {
    return 'Ok'
}

