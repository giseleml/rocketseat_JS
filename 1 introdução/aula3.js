// FUNÇÕES

function soma(num1, num2) {
    var resultado = num1 + num2;
    return resultado;
}

soma(1,2); //Execução da Função

//Lembrando: variável definida como var só é definida dentro do seu escopo, não sendo visivel fora dele.
//EX: 'resultado' não é acessível fora da função soma.
